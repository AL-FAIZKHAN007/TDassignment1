# TDassignment1
TASK1: Performs the basic mathematical operations on two numbers.
        -Asking user to enter the number 2 times.
        -Two numbers are stored in variables firstNumber and secondNumber. These values are used for all mathematical operations.
        -Performing Addition, Substraction, Multiplication & Division.
        -Printing the results.
        -Each print statement shows what operation was performed and its result.


TASK2: Create a Personalized Greeting.
        -Asking user to enter the names 2 times.
        -Two names are stored in variables firstName and lastName.
        -Those names were concatenated & stored in variable fullName.
        -Printing the greeting message using fullName.